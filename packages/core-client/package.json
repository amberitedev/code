{
	"name": "@amberite/core-client",
	"version": "0.1.0",
	"description": "Typed HTTP + WebSocket client for Amberite Core. Tauri-free — usable in any JS environment.",
	"main": "./src/index.ts",
	"scripts": {
		"lint": "eslint . && prettier --check .",
		"fix": "eslint . --fix && prettier --write .",
		"test": "vitest run",
		"test:watch": "vitest"
	},
	"devDependencies": {
		"@modrinth/tooling-config": "workspace:*",
		"vitest": "^3.2.0"
	}
}
